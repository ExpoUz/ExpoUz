"use client";

// Pitch Portal UI primitives now come from the shared @expouz/ui library so
// both admin surfaces stay visually identical. Re-exported here to keep the
// existing "@/components/ui" import paths working.
export { PageHeader, StatCard, Spinner, EmptyState, StatusPill } from "@expouz/ui";
